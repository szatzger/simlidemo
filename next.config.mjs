// Ez a fájl már nem használt, helyette a next.config.js fájlt használjuk
// Megtartjuk ezt a fájlt referenciának, de nem használjuk
/** @type {import('next').NextConfig} */

const nextConfig = {
    reactStrictMode: true,
  }
export default nextConfig;
